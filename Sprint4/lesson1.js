/*
    Names can contain letters, digits, underscores, and dollar signs
    Names must begin with a letter. NAmes cna also begin with $ and _
    Names are case sensitive (y and Y are different variables)
    Reserved words (like JavaScript keywords) cannot be used as names
*/
var name = "iCode"


//Arrays

var myTopThreeAnimals = ["Cats", "Sloths", "Snakes"];
var TopAnimals = myTopThreeAnimals[0]; // Accessing an Array elements using []
print(myTopThreeAnimals.length); // Finding the length of array using .length funciton

//Objects
var cat = {
    "age": 3,
    "name": "Happy",
    "color": "Black"
}